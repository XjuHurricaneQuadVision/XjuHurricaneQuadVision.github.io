> 作者：新疆大学 计算机科学与技术 黄耀增

>简介：这个文档包含一些在学习过程中的一些不常见但是实用的命令行指令，可以作为一个拓展了解和学习。



# 一、编译部分

## （一）并行编译

之前在一次编译大型项目工作空间时候，遇到过一次编译超时还是内存爆掉具体我忘记了，当时在网上检索时候了解到还可以控制同时编译数量，得到以下两行命令：

```Shell
colcon build --parallel-workers 6    #设置并行编译数量
colcon build --executor sequential  #关闭并行编译
```

## （二）

